{
  "name": "@submodule/core",
  "version": "3.2.0",
  "description": "Structural way to build node and deno application",
  "author": "Lagz0ne <duke@silentium.io>",
  "repository": {
    "type": "git",
    "url": "https://github.com/submodule-js/submodule",
    "directory": "pkgs/core"
  },
  "keywords": [
    "ioc",
    "configuration",
    "structure",
    "composition",
    "reusable",
    "foundation",
    "submodule"
  ],
  "license": "MIT",
  "homepage": "https://submodule.js.org",
  "dependencies": {
    "debug": "^4.3.4",
    "ts-toolbelt": "^9.6.0"
  },
  "scripts": {
    "dev": "yarn run -T tsup --watch",
    "core:dev": "yarn dev",
    "test": "vitest run",
    "core:test": "yarn test",
    "build": "yarn run -T tsup",
    "core:build": "yarn build",
    "preview": "yarn run -T standard-version --path . --dry-run -t @submodule/core",
    "core:preview": "yarn preview",
    "bump": "yarn run -T standard-version --path . -t @submodule/core",
    "core:bump": "yarn bump",
    "release": "git push --follow-tags origin main && npm publish --access public",
    "core:release": "yarn release"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "tsup": {
    "entry": [
      "src/index.ts"
    ],
    "minify": false,
    "treeshake": false,
    "splitting": false,
    "sourcemap": true,
    "clean": true,
    "dts": true
  },
  "devDependencies": {
    "@types/debug": "^4.1.7",
    "@types/node": "^18.11.18",
    "@vitest/coverage-c8": "^0.31.0",
    "@vitest/ui": "^0.31.0",
    "esbuild": "^0.16.13",
    "tsup": "^6.7.0",
    "typescript": "^4.9.5",
    "vitest": "^0.31.0"
  }
}
